{
  "name": "painel-midia-facilzap",
  "version": "1.0.0",
  "description": "Proxy para a API FacilZap",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
